apiVersion: v2
name: opsradar
description: |
  OpsRadar — open-source internal operations platform. Deploys the
  FastAPI backend, Celery worker + beat, PostgreSQL, Redis, and the
  React frontend as a single Helm release.
type: application
version: 0.1.0
appVersion: "0.1.0"
keywords:
  - process-mining
  - bpm
  - analytics
home: https://github.com/Absolute-Martial/Ops-Radar
sources:
  - https://github.com/Absolute-Martial/Ops-Radar
maintainers:
  - name: OpsRadar
